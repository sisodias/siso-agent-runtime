#!/usr/bin/env node
import { main } from "../src/cli.mjs";

try {
  process.exitCode = await main();
} catch (error) {
  process.stderr.write(`siso-agent-runtime: ${error.message}\n`);
  process.exitCode = 1;
}
